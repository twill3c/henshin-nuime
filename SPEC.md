# SPEC.md — henshin-nuime(変身の縫い目)

<!-- scaffold template v1.30.0 から展開(2026-09-05)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

カフカ『変身』を独語原文・英訳・日本語訳の三面で並べ、**どの文がどの文に対応するか
(縫い目)を教師なしで引かせ、その縫い目を測る**解剖台。深層学習の実装訓練を兼ねる。

見せたいのは訳の巧拙ではなく、**対応そのものが機械に見えるかどうか**である。
利用者には「同じ一文が三つの言語でどう姿を変えるか」が、糸で結ばれた三列の本文として見える。

## 2. 素材(2026-09-05 実測)

| 版 ID | 言語 | 版・訳者 | 出典 | 権利 |
|---|---|---|---|---|
| `de_pg22367` | 独 | Kurt Wolff Verlag 1915(初版) | [PG #22367](https://www.gutenberg.org/ebooks/22367) | カフカ 1883–1924、没後 100 年超で消滅 |
| `en_pg5200` | 英 | David Wyllie 訳 | [PG #5200](https://www.gutenberg.org/ebooks/5200) | PG が公有として配布 |
| `ja_aozora49866` | 日 | 原田義人 訳(底本『世界文学大系58 カフカ』筑摩書房 1960-04-10) | [青空文庫 49866](https://www.aozora.gr.jp/cards/001235/card49866.html) | 原田義人 1918-08-05〜1960-08-01、旧法の死後 50 年で 2010-12-31 満了 |

**外挿検証セット(held-out、L11 で使用)**: 同じ三者による『審判 / Der Prozess / The Trial』
([PG #69327](https://www.gutenberg.org/ebooks/69327) / [PG #7849](https://www.gutenberg.org/ebooks/7849) /
[青空文庫 49863](https://www.aozora.gr.jp/cards/001235/files/49863_41911.html))。
2026-09-05 に三点とも HTTP 200 で到達可能であることのみ確認済み。**中身は未検分**。

### 2.1 第二英訳を持たないこと(設計変更の記録)

構想段階では「英訳 2 種 × 和訳 2 種」の 2×2 で**訳者差と言語差を直交させる**計画だった。
L0 の実測で PG #26298 が**テキストではなく LibriVox の朗読音声**だと判明し
(証拠を `data/raw/pg26298_readme.txt` に採録。配布物は mp3 / ogg / m4b のみで本文ファイルは無い)、
Wikisource が挙げる A. L. Lloyd 訳(1937)・Stanley Corngold 訳(1996)はいずれも権利存続のため、
**公有の第二英訳は存在しない**。したがって:

- **訳者差**は日本語側だけで測る(原田訳 対 自前訳)。
- 失われた対照の代わりに、**別作品での外挿**(『審判』・訳者は同一)を置く。
  「合わせた標本で検証しても何も分からない」(HC-073)への備えとしては、こちらの方が強い。

## 3. 構造の実測(2026-09-05)

`python pipeline/ingest.py` の出力。母集団は各版の本文全体(前付・奥付を除く)。

| 版 | 段落 | 章別 | 本文字数 | 開き引用符 | 章別 |
|---|---|---|---|---|---|
| `de_pg22367` | 97 | [30, 29, 38] | 120,918 | `»` 131 | [59, 20, 52] |
| `en_pg5200` | 97 | [30, 29, 38] | 118,202 | `“` 131 | [60, 19, 52] |
| `ja_aozora49866` | 165 | [63, 36, 66] | 56,275 | `「` 129 | [58, 20, 51] |

読み取れること:

- **独と英は段落構造が完全に一致する**(総数も章別も)。Wyllie は段落を割っても繋いでもいない。
  これは学習にも推論にも一切渡さない、独立した構造オラクルになる。
- **日本語は独より細かい**(165 対 97)。原因は日本語組版の習慣で、会話文が行立てになるため。
  実測: 本文行 165 件の行頭は全角空白 92 件・鉤括弧 73 件・**その他 0 件**。
  したがって「段落が一致する」ではなく「**日本語の段落は独の段落の細分である**」という
  より弱い述語しか使えない(G-07)。
- 引用符は三版とも開きと閉じが同数(均衡)。独英は総数が一致するが章別で 1 件ずれる
  (第1章 59 対 60、第2章 20 対 19)。日本語は 2 件少ない — 地の文に溶かした直接話法がある。

### 3.1 文(L1 実測・2026-09-05)

`python -m pipeline.sentences` の出力。取りこぼし 0(区間の外に落ちるのは空白だけ)。

| 版 | 文 | 章別 | 段落あたり | 中央値 |
|---|---|---|---|---|
| `de_pg22367` | 696 | [275, 184, 237] | 7.18 | 144 字 |
| `en_pg5200` | 771 | [292, 218, 261] | 7.95 | 132 字 |
| `ja_aozora49866` | 1227 | [447, 380, 400] | 7.44 | 39 字 |

**日本語は独語の 1.76 倍の文に割れている。** 段落の細分(165 対 97)と同じ方向で、
訳出のたびに一文が複数文になっている。中央値の差は表記体系の違いも含むので、
字数の比較は言語をまたいでは意味を持たない。

各言語の分割規則が担っている仕事の量(規則を外したときの増分)は、
その言語を単独で走査して数えた現象の件数と**完全に一致する**:

| 版 | 規則 | 規則を外すと | 増分 | 独立に数えた現象 |
|---|---|---|---|---|
| 独 | 小文字継続・ダッシュ挿入句 | 726 | 30 | 直後が小文字 27 + ハイフン 3 |
| 英 | 略語 `Mr.` / `Mrs.` | 802 | 31 | `Mr.` 21 + `Mrs.` 10 |
| 日 | 引用の助詞 `と` / `など` | 1312 | 85 | `」と` 84 + `」など` 1 |

これは二経路一致である。片方は分割器の挙動、もう片方は本文の単純な数え上げで、
互いを参照していない。

### 3.2 長さだけで引いた縫い目(L2 実測・2026-09-05)

`python -m pipeline.evaluate` の出力。手法は Gale & Church (1993) の長さモデル。
アライナには**文の本文しか渡していない**(G-03)。段落はここで初めて登場する。

| 組 | c |
|---|---|
| 独→英 | 0.9768 |
| 独→日 | 0.4675 |
| 英→日 | 0.4786 |

**数値の表は §3.4 に一本化した。** L4 で埋め込みの手法が加わり、同じ組と同じ手法の
値を二箇所に書くと片方だけが古びる。伸縮率 `c` だけはこの節が出どころなので残す。

読み取れること:

- **独↔英では長さだけで縫える。** 段落一致率 0.9949 に対し、位置だけの対角線は 0.5577。
  段落境界はアライナに一度も渡っていないので、これは held-out の数字である。
- **独↔日では長さは効かない。** 細分の破れが 56 対 57 で、対角線とほぼ区別がつかない。
  英↔日は 55 対 65 でわずかに勝つが、独↔英で見た差とは桁が違う。
  **表記体系をまたぐと文字数は対応の手がかりにならない**、というのが L2 の結論である。
  これは L3 で埋め込みを持ち込む理由を、仮定ではなく実測で与える。
- 段落一致率は**独↔英でしか定義できない**(両版の段落構造が一致するため)。
  日本語を含む組では細分の破れで代用するが、この数はアライナの誤りと
  「日本語は独語の段落の細分である」という仮説の誤りを混ぜて測っている。
  だから対照との差だけを読み、単独の値を根拠にしない。

### 3.3 埋め込みと二実装照合(L3 実測・2026-09-06)

モデルは `paraphrase-multilingual-MiniLM-L12-v2`(BERT 12 層・隠れ 384・12 ヘッド・
語彙 250,037・117,654,272 パラメータ・絶対位置埋め込み・厳密 gelu・LayerNorm eps 1e-12)。
出力は平均プーリングのみで L2 正規化はしない(本家 `modules.json` に Normalize が無い)。

**二実装照合(G-04)。** 同じ重みを二つの経路で走らせた。経路A は onnxruntime が
Xenova の ONNX 書き出しを実行するもの、経路B は本プロジェクトが NumPy で書いた
順伝播が本家 safetensors を読んで走るもの。比べるのは**トークンごとの最終層出力**で、
プール後の 384 次元だけを見ない(平均で打ち消し合うため — HC-065)。

| 比べたもの | 最大差 |
|---|---|
| 経路A × 経路B(トークン単位・埋め草を除く 1,402 位置) | 4.768e-06 |
| 同・平均プーリング後(cos 最小 0.99999976) | 4.545e-07 |
| **陽性対照**: 経路B の重みを 1 要素 +0.05 | 9.156e-04 |

閾値は **1e-4**。観測された一致の約 21 倍、陽性対照の約 1/9 にあたる。
**完全一致は原理的に取れない**(HC-073)—— ONNX Runtime は演算を融合し加算順も変わるので、
float32 の丸めが揃わない。揃えた規則は dtype(float32)・語彙・切り詰め長(128)・
`intra_op_num_threads=1` の四つ。上の数値は 2026-09-06 の 1 回の走行の値であり、
検査が守るのは**大小関係**(一致 < 閾値 < 陽性対照)である。

**切り詰め(2026-09-06 実測)。** 系列長は本家 `sentence_bert_config.json` に従い 128 で切る。

| 版 | トークン数 中央値 | 最大 | 128 超 | 落ちるトークン |
|---|---|---|---|---|
| `de_pg22367` | 37 | 223 | 15 件(2.2%) | 514 / 30,574(1.68%) |
| `en_pg5200` | 34 | 180 | 7 件(0.9%) | 216 / 29,593(0.73%) |
| `ja_aozora49866` | 25 | 199 | 4 件(0.3%) | 161 / 35,939(0.45%) |

この表は一度**測り違えて出し直した**。`tokenizer.json` には truncation(128)と padding が
焼かれており、読み込んだだけのトークナイザで長さを測ると**全系列が 128 に揃う**。
その値から「128 を超えたものは 0 件」というもっともらしい嘘が出て、検査は全部緑のまま
manifest に焼かれかけた(HC-174 の系列)。測定用は truncation と padding を明示的に
外したものを使い、外れていることを T-037 が確かめる。

### 3.4 埋め込みで引いた縫い目(L4 実測・2026-09-06)

`python -m pipeline.evaluate` の出力。三手法を**同じ段落オラクル**で測った。
アライナに渡るのは、長さモデルなら文の本文、埋め込みなら 384 次元のベクトルだけで、
どちらにも段落・章・識別子は渡らない(G-03)。

| 組 | 手法 | 対応 | 段落一致率 | 細分の破れ | 被覆(src/dst) |
|---|---|---|---|---|---|
| 独→英 | embedding | 754 | **1.0000** | 0/97 | 0.997/0.970 |
| 独→英 | gale_church | 788 | 0.9949 | 3/97 | 1.000/1.000 |
| 独→英 | diagonal | 771 | 0.5577 | 64/97 | 1.000/1.000 |
| 独→日 | embedding | 939 | — | **1**/158 | 0.996/0.765 |
| 独→日 | gale_church | 1228 | — | 56/165 | 1.000/0.998 |
| 独→日 | diagonal | 1227 | — | 57/165 | 1.000/1.000 |
| 英→日 | embedding | 1012 | — | 2/160 | 0.992/0.813 |
| 英→日 | gale_church | 1234 | — | 55/165 | 1.000/1.000 |
| 英→日 | diagonal | 1227 | — | 65/165 | 1.000/1.000 |

**被覆率が手法ごとに違うので、そのまま並べてはいけない。** 埋め込みの手法は
偶然の水準を超えない対応を組まないため、日本語側で 23.5% の文を飛ばしている。
飛ばすほど破れは減って見えるので、**全手法が覆った部分だけ**で測り直した:

| 組 | 共通部分 | embedding | gale_church | diagonal |
|---|---|---|---|---|
| 独→英 | src 文 694 件 / dst 段落 97 件 | 一致率 1.0000・破れ 0 | 0.9949・3 | 0.5599・64 |
| 独→日 | src 文 693 件 / dst 段落 158 件 | 破れ **1** | 56 | 57 |
| 英→日 | src 文 765 件 / dst 段落 160 件 | 破れ **2** | 55 | 65 |

結論は変わらない。読み取れること:

- **独↔英は埋め込みで完全に縫える。** 754 本の対応が**一本残らず**対応する段落に落ちた。
  ただし**長さモデル(0.9949)との差は偶然で説明できる**(§3.5)。
  1.0000 対 0.9949 は共通部分 694 文のうち 4 文の違いにすぎない。
  差が言えるのは位置だけの対角線 0.5599 に対してだけである。
- **長さが効かなかった独↔日で、埋め込みは効く。** 同じ 158 段落で 56 対 1。
  L2 の結論(表記体系をまたぐと文字数は手がかりにならない)は、
  **手がかりが無いのではなく文字数という手がかりが無いだけ**だった。
- **1.0000 は物差しの縮退ではない。** 対の埋め込みを無作為に入れ替えると
  段落一致率は 0.0087〜0.0167 に落ちる(3 回試行、2026-09-06)。

**偶然の水準の出どころ。** 対応の良し悪しは「無関係な組がどれくらい似て見えるか」を
基準に測る。この水準は全対の cos の平均(独英 0.2652 / 独日 0.2575 / 英日 0.2212)で、
**対応を一切使わずに計算できる**。飛ばす操作は 0 点なので、偶然の水準を超えない対応は
組まれない。**調整できるつまみを一つも置いていない。**

### 3.5 差は偶然で説明できるか(L5 実測・2026-09-06)

**対応のある置換検定。** 同じ単位を二つの手法が別々に処理した結果が手元にあるので、
帰無仮説は「どちらの手法で処理したかは、その単位の出来不出来に関係ない」になる。
手法の札を入れ替えて差の分布を作り、観測した差がどれくらい端にあるかを見る。

**単位と並べ替えのブロックは、結果を見てから選んでいない。**

- 段落一致率 — 単位は **src 文**、ブロックは **その文が属する段落**。
  対応そのものを単位にすると、同じ文から出た複数の対応が独立でないまま数に入る。
  文どうしも独立でない(単調な DP は隣に引きずられる)ので、段落でまとめて入れ替える。
- 細分の破れ — 単位は **dst 段落**。ブロックの大きさだけは本文の構造から決まらないので、
  **1 / 5 / 10 の三通りで出して幅に対する頑健さを見る**(一つに決めない)。

Monte Carlo は 10,000 回。p の下限は 1/(10,000+1) なので、それを下回る値は `< 0.0001` と書く。

| 組 | 比較 | 差 | p |
|---|---|---|---|
| 独→英 | 一致率 embedding − diagonal | +0.4524 | < 0.0001 |
| 独→英 | 一致率 embedding − gale_church | +0.0058 | **0.245** |
| 独→英 | 破れ embedding − gale_church(ブロック 1/5/10) | +0.0309 | **0.255 / 0.251 / 0.255** |
| 独→英 | 破れ embedding − diagonal(ブロック 1/5/10) | +0.6598 | < 0.0001 / < 0.0001 / 0.0020 |
| 独→日 | 破れ embedding − gale_church(ブロック 1/5/10) | +0.3481 | < 0.0001(三通りとも) |
| 独→日 | 破れ embedding − diagonal(ブロック 1/5/10) | +0.3544 | < 0.0001(三通りとも) |
| 英→日 | 破れ embedding − gale_church(ブロック 1/5/10) | +0.3312 | < 0.0001(三通りとも) |
| 英→日 | 破れ embedding − diagonal(ブロック 1/5/10) | +0.3937 | < 0.0001(三通りとも) |

**読み取れること。**

- **埋め込みが長さモデルに勝つと言えるのは、日本語を含む組だけである。**
  独↔英では p = 0.245 / 0.255 で、1.0000 対 0.9949 の差は偶然で説明できる。
  §3.4 を最初に書いたときは「長さモデルを上回り」と書いていたが、**検定に通らないので直した**。
- 独↔日・英↔日はブロックの幅を 1 から 10 まで動かしても p < 0.0001 で、
  **並べ替えの単位の取り方に依存しない**。
- 独→英の「破れ embedding − diagonal」だけ、ブロック 10 で p が 0.0020 に上がる。
  ブロックが 10 個しかないと入れ替えの通り数が 2^10 = 1,024 しかなく、
  **p の分解能そのものが落ちる**。値の大小ではなく分解能の話である。

**この節の検定は確認であって、事前登録した予測の検定ではない。** L4 の数値を見た後で
当てているので、ここで出る p 値は「見えている差が偶然で説明できるか」までしか言わない。
事前登録した予測に当てるのは G-08(目玉)である。

### 3.6 目玉の設計 — 学習対をどう作るか(L6・事前登録)

**ここが循環するかしないかの分かれ目である。** 自作 Transformer に何を「対」として
見せるかで、cross-attention が段落を守るのは当たり前にもなるし、発見にもなる。

考えられる作り方と、それぞれが漏らすもの:

| 学習対の作り方 | 漏れるもの | 採否 |
|---|---|---|
| 段落どうしを対にする | **段落境界そのもの**。評価が恒等式になる | 不可 |
| 埋め込み/長さモデルの対応を対にする | その手法が既に段落を 99.5% 守っている(§3.4)ので、それを継承するだけ | 不可 |
| **文番号の比例で切ったチャンク** | **位置だけ**。これは対照(対角線)そのものである | **採用** |

採用した作り方では、モデルが対応について知りうるのは「だいたいこの辺り」という
位置情報だけで、それは**対照が既に持っている情報**である。したがって
attention が対角線を上回れば、上回ったぶんは**本文そのものから来たと言える**。

具体的には、独 696 文と英 771 文をそれぞれ K 個のチャンクに比例配分し、
`i` 番目どうしを対にする。**段落・章・文の識別子・他手法のアラインメントは
学習の入力にも損失にも一切入らない。**

**事前に書いておく予測(G-08)。**

> 一冊(独 30,574 トークン / 英 29,593 トークン)で学習した小さな Transformer は、
> **翻訳としては使いものにならない**。しかしその cross-attention から引いた
> 独↔英の文どうしの対応は、**段落番号比例の対角線を置換検定で p<0.01 で上回る**。

落ちた場合の扱いは §6 のとおり —— 主張を「注意は縫い目を学ばない」に置き換えて
図録に載せ、主画面は埋め込み由来の縫い目で成立させる。**予測が外れたら、
予測のほうを残して結果を併記する。**

### 3.7 目玉の判定 — **落ちた**(L6 実測・2026-09-07)

事前登録した予測(§3.6)は二つの部分からなる。**前半は成立し、後半は落ちた。**

**前半「翻訳としては使いものにならない」— 成立。** 30 エポックで損失 5.9421 → 2.4442
(パープレキシティ約 11.5)。貪欲復号の出力は英語として流暢だが、内容が原文と無関係である:

| | |
|---|---|
| 独語入力 | `als gregor samsa eines morgens aus unruhigen träumen erwachte , fand er sich …` |
| 正解英訳 | `one morning , when gregor samsa woke from troubled dreams , he found himself …` |
| 生成 | `" what ' s no more than i ' t have to go to go about it . " , and <unk> . samsa , …` |

一冊(独 30,574 トークン)では、英語の語の並びは覚えても、独語との対応は覚えない。

**後半「cross-attention は対角線を p<0.01 で上回る」— 落ちた。**

| 手法 | 対応 | 段落一致率 | 共通 360 文での一致率 | 被覆(src/dst) |
|---|---|---|---|---|
| attention | 360 | 0.5694 | 0.5694 | 0.517/0.467 |
| diagonal | 771 | 0.5577 | 0.5379 | 1.000/1.000 |

対応のある置換検定(単位 src 文・ブロック 段落・95 ブロック):
**差 +0.0417、p = 0.105。** 事前に決めた閾値 p<0.01 に届かない。

**したがって主張を差し替える。**

> 一冊だけで学習した小さな Transformer の cross-attention は、
> **段落境界を対角線より上手に守るとは言えない**。向きは正(+0.0417)だが、
> 偶然で説明できる範囲にとどまる。

**予測は消さない。** §3.6 に書いた予測はそのまま残し、この節を併記する。
主画面(F-07 / F-08)は §3.4 のとおり**埋め込み由来の縫い目で成立させる**。

**判定の条件は結果を見てから変えていない。** 集約は「層とヘッドで平均」、
偶然の水準は窓の構造から決まる 1/4、エポック数は 30、検定の単位とブロックは §3.5 と同じ。
層・ヘッドごとの内訳は**所見として別に出す**(§3.8)—— 良いヘッドを後から選べば、
それは選び方が結果を作っているだけである。

### 3.8 所見 — どのヘッドも学んでいない(探索・判定には使わない)

§3.7 が落ちたとき、まず疑うべきは「**平均で薄まったのではないか**」である。
翻訳モデルでは対応を担う head が特定の層に偏ることが知られている。
そこで層 × ヘッドの 12 通りを個別に測った(2026-09-07 実測):

| | ヘッド0 | ヘッド1 | ヘッド2 | ヘッド3 |
|---|---|---|---|---|
| 層0 | 0.5675 | 0.5525 | 0.5601 | 0.5357 |
| 層1 | 0.5337 | 0.5604 | 0.5663 | 0.5435 |
| 層2 | **0.5852** | 0.5549 | 0.5559 | 0.5615 |

対照(対角線)は 0.5577。**12 通りのどれも対照の近傍に散っており、突出したヘッドは無い**
(0.5337〜0.5852)。平均が薄めたのではなく、**どのヘッドも対応を学んでいない**。

**最良の 0.5852 を成績として読んではならない。** これは 12 回試した最大値であり、
選んだぶんだけ上に振れる。judgement は §3.7 の事前登録した集約のままである。

この表は、目玉が落ちた理由についての**所見**であって、目玉を救う材料ではない。

## 4. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 三版を章 → 段落 → 文の共通構造に落とす取り込み器 | must |
| F-02 | 権利台帳(版ごとの根拠・出典 URL・実測日・表示義務)を持ち、公開面に表示する | must |
| F-03 | 多言語文埋め込みによる縫い目(独↔英、独↔日、英↔日)を単調整合で引く | must |
| F-04 | 自作 Transformer(multi-head attention を素で実装)を『変身』の対訳で学習し、cross-attention から縫い目を取り出す | must |
| F-05 | 縫い目の一致度を、対角線ベースラインとの差として置換検定で評価する | must |
| F-06 | 独語原文からの自前和訳(全 97 段落)を作り、原田訳と併記する | must |
| F-07 | 画面「三面の本文」— 最大 3 列を選び、ホバーで対応段落を糸で結ぶ | must |
| F-08 | 画面「縫い目の地図」— アラインメント行列と単調経路、埋め込み由来と注意由来の重ね表示 | must |
| F-09 | 画面「注意の帯」— 層/ヘッド/学習ステップを切り替えて cross-attention を見る | must |
| F-10 | 画面「一語の変身」— `ungeheueren Ungeziefer` ほか訳が割れた語を三言語で並べる | must |
| F-11 | 画面「ずれの図録」— 分割/併合の位置、長さ比、細分性、三角整合率 | should |
| F-12 | 画面「歩き方 / 設計図」— 手法と、測って捨てた主張の記録 | must |
| F-13 | 任意の文を入力して三言語の該当箇所へ飛ぶブラウザ内検索 | should |
| F-14 | 『審判』での外挿検証を回し、結果を図録に併記する | should |

## 5. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | cron・DB・サーバ関数を持たない静的サイトとして公開する | ビルド成果物に関数が無いことを検査(G-11) |
| N-02 | 学習・アラインメントはビルド前のオフライン工程で完結し、公開面は焼き込み済みの成果物だけを読む | パイプラインと公開面のディレクトリ分離 |
| N-03 | ブラウザ内推論に使うモデルの転送量に上限を設ける | 実測して SPEC に置く(L2 以降・**未実測**) |
| N-04 | 三版の本文と自前訳を、字種衛生検査の対象に含める | `harness/text_hygiene.py`(G-02) |

## 6. 目玉(事前登録)

> **『変身』一冊だけで学習した小さな Transformer は、翻訳はできない。
> しかしその cross-attention が引く独↔英の対応は、段落番号比例の対角線を
> 置換検定で有意に上回る。**

判定は G-08。**落ちた場合の扱いを先に決めておく**:

- 目玉が落ちたら、主張を「注意は縫い目を学ばない」に置き換えて図録に載せる。
- 主画面(F-07/F-08)は**埋め込み由来の縫い目で成立させる**。アプリは死なない。
- 落ちた予測は消さず、実測と併記して残す。

## 7. 品質ゲート

| ID | ゲート | 判定 | 状態 |
|---|---|---|---|
| G-01 | 三版の権利根拠・出典・実測日が台帳にあり、PG のヘッダ/フッタが本文から除去され、青空の記載事項(作品名・著者名・翻訳者名・底本・入力者・校正者・作成/修正日)が保持されている | 台帳と本文の突合 | L0 で実装 |
| G-02 | 本文・自前訳・ソースに異種文字・制御文字が混入していない | `text_hygiene` | L0 で実装 |
| G-03 | **循環の禁止** — 章境界・段落境界は縫い目の入力に一切渡らない | `pipeline/align.py` がプロジェクト内モジュールを import せず、段落・章・識別子の属性に触れないことを AST で検査。加えて、メタデータを入れ替えても縫い目が変わらないことを実際に確かめる | L2 で実装 |
| G-04 | **二実装照合** — 文埋め込みを onnxruntime(経路A)と自前の NumPy 順伝播(経路B)で計算し、**トークンごとの最終層出力**が一致する。プール後の値だけを見ない(HC-065) | 実測した閾値との比較。**陽性対照**として重みを 1 要素ずらすと落ちること | L3 で実装 |
| G-05 | 目玉の判定には必ず対角線ベースラインを併記する | 評価器が全ての組について対照を返さなければ落とす。対照が本当に対照になっている(手法と結果が違う)ことも固定する | L2 で実装 |
| G-06 | `de→ja` と `(de→en)∘(en→ja)` の三角整合率を出す | 値は実測して置く | L7 で実装(**未実装**) |
| G-07 | **細分の破れを対照つきで測る** — 日本語の一段落が独語の複数段落にまたがった件数を、手法と対角線の両方で出す。**この数はアライナの誤りと「細分である」という仮説の誤りを混ぜて測っている**ので、単独の値ではなく対照との差だけを読む | 縫い目の写像を検査し、両方の値を必ず併記する | L2 で実装 |
| G-08 | **目玉** — 注意由来の縫い目が対角線ベースラインを置換検定 p<0.01 で上回る | 置換検定 | L6 で実装・**不通過**(差 +0.0417 / p=0.105、§3.7)。閾値は据え置き、判定を図録に載せる |
| G-09 | 『審判』で同じ手続きを回し、結果を併記する | 外挿の実測 | L11 で実装(**未実装**) |
| G-10 | 図の要素が `viewBox` に収まる(`getBBox` 比較) | 実ブラウザ検品 | L8 で実装(**未実装**) |
| G-11 | 公開成果物に cron・DB・サーバ関数が無い | ビルド成果物の走査 | L10 で実装(**未実装**) |
| G-12 | 自前訳の主要語が訳語登録簿と照合され、未登録の揺れで落ちる | 登録簿との突合 | L9 で実装(**未実装**) |
| G-13 | 自前訳の段落数が独語原文と一致し、固有名・数値が悉皆で照合される | 落丁検査 | L9 で実装(**未実装**) |
| G-14 | **文分割の取りこぼしが無い** — 文の区間の外に落ちるのは空白だけで、区間から段落を復元できる | `check_lossless` が例外で落とす | L1 で実装 |
| G-15 | **分割規則の言語独立性** — 各言語の分割規則は、その言語の本文だけを見て決める。他言語の分割結果を見て規則を足さない | 各規則の寄与が、その言語を単独で数えた現象の件数と一致することを検査 | L1 で実装 |
| G-16 | **正規化の置換が意図したものだけである** — 入力と出力の文字種の差分を出し、意図しない置換が 1 件でもあれば落とす | 文字ヒストグラムの差分 | L1 で実装(HC-174) |
| G-18 | **三本目の経路** — ブラウザ内推論(transformers.js / 同じ ONNX)の埋め込みが、経路A と一致する | 実ブラウザで少数の文を通し、焼いた埋め込みと突き合わせる | L8 で実装(**未実装**) |
| G-21 | **自作 attention の勾配が数値微分と一致する** — multi-head attention を素で書く以上、順伝播が合っていても逆伝播が合っている保証はない。中心差分と突き合わせる | 倍精度の小さなモデルで、無作為に選んだパラメータ成分の解析勾配と中心差分の相対誤差を測る。**陽性対照**として、勾配を意図的にずらすと落ちる | L6 で実装 |
| G-19 | **検定器そのものを確かめる** — Monte Carlo の p 値が、ブロック数の少ない場合の**全数列挙**と一致する。統計の道具はもっともらしい値を返すので、別経路で見ないと合っているか分からない | 12 ブロック以下の合成データで両者を突き合わせ、**陽性対照**として差の無いデータで p が 1 に近くなることも確かめる | L5 で実装 |
| G-20 | **並べ替えの単位を結果から選ばない** — 段落一致率は「文を単位・段落をブロック」で本文の構造から決まる。細分の破れはブロック幅が構造から決まらないので、**複数の幅で出して一つに畳まない** | 評価器が `REFINEMENT_BLOCK_SIZES` の全ての幅について p を返さなければ落とす | L5 で実装 |

**この表は宣言であって実装ではない**(HC-157)。各 `G-xx` は TEST_SPEC のケースから ID で
参照されるか、上の「状態」欄に**未実装**と明記されているかのどちらかでなければならない。

## 8. 循環の禁止(この企画の背骨)

測るものと、測る道具を分ける。具体的には:

1. **段落・章の境界は縫い目の入力に渡さない。** 渡すと「境界を守る縫い目が引けた」は恒等式になる。
   これは約束ではなく**構造**で守る —— `pipeline/align.py` はプロジェクト内のモジュールを
   一切 import せず、標準ライブラリだけで閉じている。段落を知っている型に触れる道が
   構文として存在しない。本文を読む口(`main`)も置かない。T-024 が AST で、
   T-025 がメタデータの入れ替えで、T-030 が入口の不在で、それぞれ確かめる。

   **避けようのない漏れが一つある。** 文は段落をまたがないので、
   **段落境界は文境界の部分集合である**。ただし「どの文境界が段落境界でもあるか」は
   入力に現れないので、評価に使う情報がアライナに渡っているわけではない。
   この漏れを消す方法は無い(段落をまたぐ文分割は原文の側が許さない)ので、
   消せないことを書いておく。

   パラメータの出所も分ける。伸縮率 `c` は**本文の総字数の比だけ**から出す(対応を使わない)。
   分散 `s²` は Gale & Church (1993) の公表値 6.8 を、**この corpus に当てはめ直さずに**使う。
   当てはめるには対応が要り、対応こそが測りたいものだからである。
2. **自前訳は縫い目の測定対象から外す。** 訳出者(このプロジェクト)は縫い目の結果を見られる立場に
   あるため、測定に使うと循環する。自前訳は**表示**と**訳者差の参考値**にのみ用いる。
3. **自前訳は段落単位で独語原文だけを見て訳す。** 既存の三訳もアラインメント出力も参照しない。
   訳出完了後にハッシュで凍結し、以後、測定結果を見て訳文を直さない。

## 9. ループ計画

| | 内容 | 主なゲート | 状態 |
|---|---|---|---|
| L0 | 権利実測・三版取得・章/段落への分割・SPEC/TEST_SPEC 起草 | G-01, G-02 | **完了(2026-09-05)** |
| L1 | 文分割(三言語)と被覆の実測、正規化の置換検査 | G-14, G-15, G-16 | **完了(2026-09-05)** |
| L2 | 専用 venv・単調アラインメントの DP・対照 2 本(対角線 / 長さモデル)・循環の禁止 | G-03, G-05, G-07 | **完了(2026-09-05)** |
| L3 | 埋め込みの入手と**二実装照合**(onnxruntime × 自前 NumPy 順伝播)・全文の埋め込み | G-04 | **完了(2026-09-06)** |
| L4 | 埋め込み由来の縫い目・三手法の並置・被覆率を揃えた比較 | G-05, G-07 | **完了(2026-09-06)** |
| L5 | 置換検定の枠組み(縫い目の差が偶然でないことを測る) | G-19, G-20 | **完了(2026-09-06)** |
| L6 | 自作 Transformer の学習と cross-attention 抽出 | **G-08(目玉判定・不通過)**, G-21 | **完了(2026-09-07)** |
| L7 | 三角整合 | G-06 | 次 |
| L8 | 画面「三面の本文」「縫い目の地図」「注意の帯」 | G-10, G-18 | |
| L9 | 自前和訳(全 97 段落)と訳語登録簿・落丁検査 | G-12, G-13 | |
| L10 | 画面「一語の変身」「ずれの図録」「歩き方」・デプロイ | G-11 | |
| L11 | 『審判』での外挿検証 | G-09 | |

**L3 でもう一つ繰り下げた。** 埋め込みを入手して二実装照合まで済ませた時点で
一区切りとし、それを使った縫い目は L4 に送った。照合(自前で BERT の順伝播を書いて
onnxruntime と突き合わせる)がこの企画の実装訓練の中身そのもので、
縫い目と一緒に一つのループに詰めると、どちらの検査も薄くなる。

**L2 で計画を一つ繰り下げた。** 当初 L2 に埋め込みまで入れていたが、長さだけの手法が
どこまで行くかを先に測ると、埋め込みを持ち込む理由が仮定でなく実測になる。実際、
独↔英は長さだけで 0.9949 まで行き、独↔日は対角線と区別がつかなかった(§3.2)。
埋め込みは L3 に送った。

**L0 で文分割を積み残した。** 当初の L0 目標には文分割まで含めていたが、段落の実測で
日本語の段落粒度が独英と異なることが分かり(§3)、文の単位をどう置くかが段落の扱いに
依存することになったため、L1 に送った。文分割は縫い目の単位そのものなので、
被覆を実測してから規則を決めた(HC-069)。L1 で完了。

## 10. スコープ外

- **翻訳器としての実用性**。自作 Transformer は「一冊で何が学べるか」を測るためのものであり、
  訳文の生成品質を売りにしない。
- **第二の英訳**。公有のものが存在しないため扱わない(§2.1)。
- **原文校訂**。PG・青空の本文をそのまま正本とし、異文の検討はしない。
- **課金の生じる推論**。外部 LLM API をビルド時・実行時とも呼ばない。
- **『審判』の全訳**。外挿検証に必要な範囲でのみ扱う。
