# TEST_SPEC.md — henshin-nuime

<!-- scaffold template v1.30.0 から展開(2026-09-05) -->

## 実行規約

- `pytest -x -q` を stage 3–5 の判定に使用。マーカー: `unit` / `integration` / `validation`
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャは、期待値の導出前提(直交性・一意性・単一帰属等)を
  **テスト内の assert で検算**し、導出過程をコメントに残す

## 期待値の出所(HC-016)

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。SPEC の保証粒度を超える期待値を書かない |
| 外部権威(公表値・規格・検証データ) | 出典 URL と取得日をフィクスチャの先頭に書く |
| 実測 | **実測日と実測値**をコメントに残す |

件数・行数は定数で書かず、**集合の一致・取りこぼしの不在**という不変量で書く。

**このプロジェクトで定数を許す唯一の場所は SPEC §3 の実測表との突合(T-013)である。**
そこでは「文書に書いた数」と「いま走査して出た数」が一致することを検査するので、
定数であることが目的になっている。それ以外のケースで件数を直書きしてはならない。

## オラクルの出所

| フィクスチャ | 出所 | 性格 |
|---|---|---|
| `data/raw/de_pg22367.txt` | Project Gutenberg #22367(2026-09-05 取得) | 外部権威・原文 |
| `data/raw/en_pg5200.txt` | Project Gutenberg #5200(2026-09-05 取得) | 外部権威・英訳 |
| `data/raw/ja_aozora49866.html` | 青空文庫 49866(2026-09-05 取得) | 外部権威・和訳 |
| 独英の段落構造の一致 | 二つの独立したファイルが互いに一致するという事実 | **非循環オラクル**(どちらも他方を参照せず作られた) |
| 引用符の開閉均衡 | 各版の内部構造 | 版ごとに閉じた検算 |
| 青空の取り扱い規準 | https://www.aozora.gr.jp/guide/kijyunn.html(2026-09-05 取得) | 外部権威・権利 |

## ケース一覧

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | F-01 / G-01 | PG のヘッダ・フッタが段落本文に残らない | `START OF THE PROJECT GUTENBERG` 等の標識文言・PG の許諾文が、どの段落にも現れない |
| T-002 | F-01 / G-01 | PG 標識の陽性対照 | START/END 標識を欠く入力に `strip_pg_wrapper` を当てると `IngestError` |
| T-003 | F-01 | 章の数と並び | 三版とも章 3・並びは独英 I/II/III、日 1/2/3。壊した入力で `IngestError`(陽性対照) |
| T-004 | F-01 | **独と英の段落構造が一致する** | 総段落数と章別段落数が両版で等しい。定数ではなく両版の相互一致として書く |
| T-005 | F-01 / G-07 | 細分性の必要条件 | 各章で 日本語の段落数 ≥ 独語の段落数 |
| T-006 | F-01 | 引用符の開閉が均衡する | 各版で開き括弧と閉じ括弧の数が等しい(独 `»«`、英 `“”`、日 `「」`) |
| T-007 | F-01 | 段落テキストの衛生 | 空文字が無い・前後に空白が無い・改行文字を含まない |
| T-008 | F-01 | ルビの取りこぼしが無い | 青空本文のルビについて、親文字が本文に残り、振仮名が本文に混入していない。ルビが 1 件以上あることも確かめる(走査対象が空でないこと) |
| T-009 | F-01 | 取り込みが決定論的である | 同じ生ファイルから二度組み立てて、結果が完全一致する |
| T-010 | G-02 | 字種衛生 | 日本語本文にキリル文字・ハングル・制御文字が無い。**陰性対照**として独語のウムラウト・ß と英語のカーリークォートは撃たれない |
| T-011 | F-02 / G-01 | 権利台帳の充足 | 台帳が三版すべてを覆い、必須欄(出典 URL・実測日・権利根拠・表示義務)が空でない。青空版は作品名・著者名・翻訳者名・底本・入力者・校正者・作成日/修正日を保持している |
| T-012 | F-02 / G-01 | 権利台帳の陽性対照 | 必須欄を落とした台帳を検査器に渡すと落ちる |
| T-013 | — | **SPEC §3 の実測表が実際の走査と一致する** | SPEC に書いた段落数・章別・字数・引用符数を、いま走査して出た値と突き合わせる(HC-152 — 文書中の数値を守るものは他に無い) |
| T-044 | — | **ゲート表とループ計画の突合** | SPEC §7 の各ゲートが書く「L*n* で実装」と、§9 のループ計画がそのゲートを挙げるループ番号が一致する。ループを繰り下げたとき、**ゲート表の参照だけが古びる**(L4 で実際に 5 箇所ずれた)。**陽性対照**として、番号をずらした表を同じ検査に掛けると落ちる |
| T-014 | — | **品質ゲートの対応検査** | SPEC §7 の各 `G-xx` は、この TEST_SPEC のケース表から参照されるか、SPEC 側に「未実装」と明記されているかのいずれかである(HC-157) |
| T-015 | F-01 / F-02 | パイプラインの起動経路が実際に走る | `python -m pipeline.ingest` と `python -m pipeline.rights` が終了コード 0 で完走する。**テストは package として import するので、CLI が死んでいても緑になる** — その穴を塞ぐケース |
| T-016 | F-01 / G-14 | 文分割の取りこぼしが無い | 全段落について、文の区間の外に落ちるのが空白だけである。**陽性対照**として、区間を人為的にずらすと `SplitError` |
| T-017 | F-01 / G-14 | 文が空でなく、前後に空白が無く、順序と非重なりが保たれる | 区間が単調増加で重ならない |
| T-018 | F-01 / G-15 | **独語の規則の寄与が、独語だけを数えた現象と一致する** | 「直後が小文字」「ハイフン挿入句」を外したときの文の増分が、本文を単純に走査して数えた件数と等しい |
| T-019 | F-01 / G-15 | **英語の略語規則の寄与が、`Mr.` / `Mrs.` の出現数と一致する** | 略語規則を外したときの増分が、語境界つきで数えた `Mr.` + `Mrs.` の件数と等しい。**陰性対照**として、略語の直後で切れた文(文頭が `Samsa`)が 0 件 |
| T-020 | F-01 / G-15 | **日本語の引用助詞規則の寄与が、`」と` / `」など` の出現数と一致する** | 引用助詞規則を外したときの増分が、本文を走査して数えた件数と等しい |
| T-021 | F-01 / G-16 | **正規化が意図した置換だけを行う** | 生の段落と正規化後の文字ヒストグラムの差分が、畳んでよい空白の除去だけである。とくに**全角空白 U+3000 が保存される**。**陽性対照**として、`\s+` で畳む旧実装は落ちる |
| T-022 | — | SPEC §3.1 の文の実測表が実際の走査と一致する | 文の総数・章別・段落あたり・規則の寄与を、いま走査して出た値と突き合わせる(HC-152) |
| T-023 | F-01 / F-02 | `python -m pipeline.sentences` が起動経路として走る | 終了コード 0(T-015 と同じ穴を文分割器にも塞ぐ) |
| T-024 | F-03 / G-03 | **循環の禁止・構造** | `pipeline/align.py` が AST 上でプロジェクト内モジュールを import せず、`chapter` / `paragraph` / `pid` / `index` / `edition_id` のいずれの属性にも触れない。**陽性対照**として、それらを含む偽のソースを同じ検査に掛けると落ちる |
| T-025 | F-03 / G-03 | **循環の禁止・振る舞い** | 文のメタデータ(章・段落・識別子)を入れ替えても縫い目が変わらない。あわせて `align` が文字列以外の列を `TypeError` で拒む |
| T-026 | F-03 | DP の正しさと被覆 | 合成データで既知の対応(1-1 / 1-2 / 2-1)を再現する。実データでは対応が `src` と `dst` を隙間も重なりも無く覆う |
| T-027 | F-03 / G-05 | **対照が本当に対照になっている** | 評価器がすべての組について対角線の結果を返す。独→英で対角線の段落一致率が手法より**低い**(対照が成り立つ前提の固定)。**陽性対照**として、対角線を手法に差し替えると差が消える |
| T-028 | F-03 / G-07 | 細分の破れを手法と対照の両方で出す | 独→日・英→日の両組で、手法と対角線の破れ件数がともに返る。片方でも欠けたら落とす |
| T-029 | — | SPEC §3.2 / §3.4 の縫い目の実測表が実際の走査と一致する | 伸縮率 `c`(§3.2)と、対応数・段落一致率・細分の破れ・被覆率(§3.4)を、いま走査して出た値と突き合わせる(HC-152)。**同じ数字が二箇所に無いこと**も併せて確かめる |
| T-030 | F-03 | `python -m pipeline.evaluate` が起動経路として走る | 終了コード 0。`pipeline/align.py` は本文を読む口を持たないので起動経路を持たない |
| T-031 | F-03 / G-04 | **二実装照合** | onnxruntime(経路A)と自前 NumPy 順伝播(経路B)の**トークンごとの最終層出力**が、実測した閾値以内で一致する。プール後の値だけでは平均で打ち消し合うので、経路の途中にあたる量を比べる(HC-065) |
| T-032 | F-03 / G-04 | **照合の陽性対照** | 経路 B の重みを 1 要素だけ +0.05 ずらすと、差が閾値を超える。ずらす前は超えないことも同じ場所で確かめる |
| T-033 | F-03 | 自前の safetensors 読み取り | 期待するテンソル名・形・dtype が読める。**陽性対照**として、見出し長を壊した入力では例外になる |
| T-034 | F-03 | `gelu` と `layer_norm` の単体検査 | 閉じた式から導いた値と一致する。`gelu` は誤差関数版であって tanh 近似版ではないことを、両者が実際に食い違う点で確かめる |
| T-035 | F-03 | 埋め込みキャッシュの整合 | manifest の文数・次元が `.npy` の形と一致し、記録した sha256 が手元のモデルファイルと一致する |
| T-036 | F-03 | `python -m pipeline.embed --stats` が起動経路として走る | 終了コード 0。全文の埋め込みは 16 分かかるので、検査には軽い方を通す |
| T-038 | — | SPEC §3.3 の切り詰めの表が実際の走査と一致する | トークン数の中央値・最大・128 超の件数・落ちるトークン数を、いま測った値と突き合わせる(HC-152) |
| T-039 | F-03 | **埋め込みが再現する** | 同じ入力をその場で二度通してビット単位で一致する(比較対象の有無に依存させない)。あわせて、焼いてあるキャッシュが**いま計算し直した値**と一致する — 16 分かけて作るキャッシュが古びていないことを少数の文で確かめる |
| T-040 | F-03 / G-03 | **埋め込みアライナも本文を受け取らない** | `align_embeddings` は数値配列だけを受け取る。文字列の列を渡すと落ちる。文のメタデータを入れ替えても縫い目が変わらない |
| T-041 | F-03 | **入れ替え対照** | 対の埋め込みを無作為に入れ替えると、独→英の段落一致率が崩れる(素の値の 1/10 未満)。1.0000 が物差しの縮退でないことの確認 |
| T-042 | F-03 / G-05 | **被覆率を揃えて比べる** | 段落一致率・細分の破れを、全手法が覆った共通部分でも出す。埋め込みの手法は日本語側で 2 割ほど飛ばすので、**飛ばすほど有利になる物差しをそのまま並べない**。共通部分の分母が手法間で一致することも確かめる |
| T-043 | F-03 | 偶然の水準が対応を使わずに出る | `chance_similarity` が全対の cos の平均に一致する(素朴な二重ループと突き合わせる)。**陽性対照**として、片方を定数ベクトルにすると水準が変わる |
| T-045 | F-05 / G-19 | **検定器の二経路照合** | 12 ブロック以下の合成データで、Monte Carlo の p 値が全数列挙の厳密値と一致する(誤差は Monte Carlo の標準誤差の範囲)。**陽性対照**として、差の無いデータでは p が 1 に近く、差の大きいデータでは小さくなる |
| T-046 | F-05 | 検定器の性質 | ブロック分けが実際に効く(同じデータを 1 ブロックにまとめると p の分解能が落ちる)。長さの違う入力・空の入力は例外で落ちる。`p_display` が分解能を下回る値を「0」と書かない |
| T-047 | F-05 / G-20 | **ブロック幅を畳まない** | 評価器が `REFINEMENT_BLOCK_SIZES` の全ての幅について p を返す。一つでも欠けたら落とす |
| T-048 | — | SPEC §3.5 の検定表が実際の走査と一致する | 差と p を、いま走らせた検定の値と突き合わせる(HC-152)。p は乱数を使うので**種を固定**し、`< 0.0001` の行は「分解能を下回る」ことだけを確かめる |
| T-049 | F-04 | **素で書いた multi-head attention の順伝播** | ヘッドごとにループで回した参照計算と一致する(同じ式を別の書き方で確かめる)。注意の重みが各行で和 1 になる。マスクした位置に注意が漏れない。**陰性対照**としてマスク無しなら同じ位置に注意が向く |
| T-050 | F-04 / G-21 | **勾配が数値微分と一致する** | 倍精度の小さなモデルで、無作為に選んだパラメータ成分(自作 attention の重みを必ず含む)の解析勾配と中心差分の相対誤差が 1e-5 未満。**陽性対照**として、勾配を 0.1% ずらすと判定が撃つ。**学習は勾配が間違っていても損失が下がる形で失敗する**ので、損失曲線では代替できない |
| T-051 | F-04 / G-03 | **学習対が位置だけで決まる** | `windows` が文の本数だけから決まり、単調で両端を覆う。帯の幅が窓幅から決まり、窓を広げれば狭まらない |
| T-052 | F-04 | **点数表が帯の外でゼロ** | cross-attention 由来の対応が、窓が許す帯の外に引かれない。帯が全面を覆っていないことも確かめる(対照の前提) |
| T-053 | F-04 / G-08 | **目玉の判定** | attention 由来の縫い目の段落一致率が、対角線を対応のある置換検定で p<0.01 で上回る。**落ちたら SPEC に結果を併記して主張を差し替える**(予測は消さない) |
| T-037 | F-03 | **長さの測定が素のトークナイザで行われる** | `tokenizer.json` には truncation(128)と padding が焼かれている。読み込んだだけのものでは長さが全部 128 に揃い、「上限超過 0 件」という嘘が出る。測定用は明示的に外したものであること。**陽性対照**として、配布物に truncation が焼かれていることを同じ場所で確かめる(前提が変わったら落とす) |
